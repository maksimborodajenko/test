export declare const MAX_BADGE_COUNT = 99;
export declare function toBadgeCount(count: number, max?: number): string;
export declare function toTooltip(count: number, max?: number): string | null;
export declare function getPaginationText({ currentPage, totalItems, itemsPerPage, currentPageCount, }: {
    currentPageCount: number;
    currentPage: number;
    totalItems: number;
    itemsPerPage: number;
}): {
    '%startNumber%': number;
    '%endNumber%': number;
    '%totalNumber%': string;
};
