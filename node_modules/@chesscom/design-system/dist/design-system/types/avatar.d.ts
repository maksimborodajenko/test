import type { mediaQueries } from '../../styles/variables';
export type CCAvatarStatus = {
    isOnline: boolean;
    isPlaying?: boolean;
    isStreaming?: boolean;
};
export type CCAvatarSize = 24 | 32 | 40 | 48 | 64 | 80 | 96 | 160;
export type CCAvatarResponsiveSizes = Record<keyof typeof mediaQueries, CCAvatarSize>;
export type ClickBehaviorTypes = 'none' | 'openProfile' | 'openProfileInNewTab';
