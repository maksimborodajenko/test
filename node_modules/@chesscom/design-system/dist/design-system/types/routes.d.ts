/** Routes needed for the design system */
export interface DesignSystemRoutes {
    webAbout: string;
    webMembership: (params: Record<string, string>) => string;
    webMemberTitledPlayers: string;
    webMemberView: (username: string | undefined) => string;
}
