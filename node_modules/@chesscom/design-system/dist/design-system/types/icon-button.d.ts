import type { TooltipAnchor, TooltipPosition } from '../../types/tooltip';
import type { DisplaySafeString } from '../../types/translations';
export type CCIconButtonIconSize = 12 | 16 | 20 | 24;
export type CCIconButtonSize = 'x-small' | 'small' | 'medium' | 'large' | 'x-large' | 'xx-large';
export type CCIconButtonVariant = 'ghost' | 'primary' | 'secondary' | 'border' | 'hovered';
export type CCIconButtonCustomClasses = {
    container?: string;
    icon?: string;
};
export interface CCIconButtonTooltipConfig {
    text: DisplaySafeString;
    anchor?: TooltipAnchor;
    closeOnClick?: boolean;
    position?: TooltipPosition;
}
