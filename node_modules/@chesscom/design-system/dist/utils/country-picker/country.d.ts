import { type CountryCode } from 'libphonenumber-js';
import { type DisplaySafeString } from '../../../utils/typescript';
export interface Country {
    code: string;
    iso: CountryCode;
    name: DisplaySafeString;
    prefix: string;
    description?: string;
    price?: number;
}
