import type { TranslatedString } from '../../../utils/typescript';
export declare const getTranslatedPhrase: (key: string, domain?: string, noDefault?: boolean) => any;
/**
 * Simple message translation with variables. Message variables are automatically sanitized.
 *
 * e.g. plain message:
 *  import { Translator } from '../translation/translation.helper';
 *  Translator . trans('You are now following %1$s%', username)
 *
 * e.g. message with variable:
 *  import { Translator } from '../translation/translation.helper';
 *
 *  Translator . trans('test %1$s% string %2$s%', {'%1$s%': first, '%2$s%': second})
 *
 * @param {string} key - message to translate
 * @param {object} params - variables to include
 * @param {string} domain - domain for variable
 *                          if none specified will check messages first, then javascript
 * @return {string} - translated message
 */
export declare const trans: (key: string, params?: Record<string, string | number> | null, domain?: string) => TranslatedString;
/**
 * Simple message translation with variables. Message variables are **NOT** automatically sanitized.
 *
 * e.g. plain message:
 *  import { Translator } from '../translation/translation.helper';
 *  Translator . unsafeTrans('You are now following %1$s%', username)
 *
 * e.g. message with variable:
 *  import { Translator } from '../translation/translation.helper';
 *
 *  Translator . unsafeTrans('test %1$s% string %2$s%', {'%1$s%': first, '%2$s%': second})
 *
 * @param {string} key - message to translate
 * @param {object} params - variables to include
 * @param {string} domain - domain for variable
 *                          if none specified will check messages first, then javascript
 * @return {string} - translated message
 */
export declare const unsafeTrans: (key: string, params?: Record<string, string | number> | null, domain?: string) => TranslatedString;
/**
 * Translation with pluralization.
 *
 * e.g.:
 *  import { Translator } from '../translation/translation.helper';
 *
 *  Translator . transChoice(
 *    '{0} 0 days|{1} 1 day|]1,Inf] %1$s% days',
 *    6,
 *    { '%1$s%': 6 }
 *  );
 *
 * @param {string} key - message to translate
 * @param {number} number - pluralization number
 * @param {object} params - variables to include
 * @param {string} domain - domain for variable
 *                          if none specified will check messages first, then javascript
 * @return {string} - translated message
 */
export declare const transChoice: (key: string, number: number, params: Record<string, string | number>, domain?: string) => any;
/**
 * Checks if given translation exists.
 * @param {string} key
 * @param {string} domain
 * @return {boolean}
 */
export declare const hasTranslation: (key: string, domain: string) => boolean;
/**
 * Returns the map of countries and its translations.
 * @return {object}
 */
export declare const getCountries: () => any;
